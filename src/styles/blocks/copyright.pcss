.copyright {
    @include color($white, 0.3);
    font-size: 48px;
    font-weight: 600;
    line-height: 40px;
    white-space: nowrap;
    padding-right: 50px;

    @include tablets {
        margin-bottom: 20px;
    }
}

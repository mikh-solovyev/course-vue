.tags {
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 35px;

    &__item {
        padding: 14px 22px;
        border-radius: 40px;
        background: $tag;
        margin-right: 8px;

        &:last-child {
            margin-right: 0;
        }
    }
}
